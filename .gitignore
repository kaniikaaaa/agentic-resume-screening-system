# python
venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/

# node
node_modules/
.next/
next-env.d.ts
*.tsbuildinfo

# static export, built by the Dockerfile for single-origin hosting
out/

# env
.env
.env.local

# vercel
.vercel

# os
.DS_Store
Thumbs.db

# The resumes under public/samples/ are synthetic fixtures, not real
# candidates — committed on purpose so the demo and tests work on a clone.

# Editor artifact: VS Code recreated this path when the nested folder was
# flattened. Nothing should live here.
agentic-resume-screening-system-main/
