fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20
pypdf==5.1.0
python-docx==1.1.2
google-genai==0.3.0

# Only imported when ORCHESTRATOR=graph. Kept in the main requirements so the
# graph route works on a deploy without a second install step.
langgraph==1.0.7
